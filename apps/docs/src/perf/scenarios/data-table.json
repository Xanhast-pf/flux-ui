{
  "id": "data-table",
  "label": "DataTable",
  "unit": "loaded rows",
  "kind": "workload",
  "maxCount": 5000,
  "fixtureRevision": 2,
  "description": "One virtual table; create and sort N loaded rows; update with a new ordering."
}
