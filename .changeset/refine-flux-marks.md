---
"@flux-ui/icons": patch
---

Refine `BellOffIcon` with a complete bell silhouette and redesign `FluxMarkIcon` as a distinctive forward-flow `F` monogram while preserving the existing 20×20 icon system and size contract.
