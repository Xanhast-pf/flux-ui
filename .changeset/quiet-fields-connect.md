---
"@flux-ui/react": minor
---

Add the compound `Field` primitive for accessible form labels, descriptions, errors, required state, disabled state, and validation wiring without adding runtime work to standalone controls.
