---
"@flux-ui/react": minor
---

Add native-first `Dialog` and `Drawer` overlay primitives plus keyboard-accessible `Tabs`, establishing the navigation and overlay foundation used by the Flux documentation shell.
