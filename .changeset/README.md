# Changesets

Add a changeset for every user-visible package change:

```bash
pnpm changeset
```

Do not add changesets for docs-only, test-only, or internal tooling changes unless they affect a published package contract.
