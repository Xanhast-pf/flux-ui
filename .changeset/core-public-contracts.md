---
"@flux-ui/react": patch
---

Export named props for every public runtime component alias and add a compiler-derived public contract that keeps component parts, DOM customization escape hatches, and typed component CSS variables synchronized with the documentation and quality gates.
