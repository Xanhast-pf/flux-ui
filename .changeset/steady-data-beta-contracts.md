---
"@flux-ui/react": minor
---

Harden data and display APIs before beta. DataTable now makes sorting and selection ownership explicit, server-owned sorting requires a request callback, LevelMeter and Sparkline reserve runtime-owned accessibility semantics, composed data-display components reject incompatible innerHTML injection, and Stat/Tag optional values are friendlier under exactOptionalPropertyTypes.
